# Copy to .env.local
ADMIN_PASSWORD=change-me
AUTH_SECRET=long-random-string

# Database connection (use either)
# If you use Marketplace providers like Neon, you'll usually get DATABASE_URL
DATABASE_URL=postgres://user:pass@host:5432/db
# Or if using Vercel Postgres, you may have POSTGRES_URL
POSTGRES_URL=postgres://user:pass@host:5432/db

# Vercel Blob token (create in Storage → Blob → Tokens)
BLOB_READ_WRITE_TOKEN=your-blob-token
